import Advertisement from "../components/Advertisement/Advertisement";

const AdvertisementContainer = (props) => {

    return (
        <Advertisement left={} right={} state={props.state}/>
    );
}

export default AdvertisementContainer;