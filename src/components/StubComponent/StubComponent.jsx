const StubComponent = (props) => {
    return (
        <div style={
            {
                height: '20px',
                width: '100%',
            }
        }/>
    )
}

export default StubComponent;